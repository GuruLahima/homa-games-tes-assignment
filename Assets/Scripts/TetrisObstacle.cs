using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Com.HomaGamesTest.Alek{
	public class TetrisObstacle : MonoBehaviour
	{
/*		private int cylinderCount;

		void OnCollisionEnter(Collision info){
			if(info.transform.GetComponent<Renderer>().sharedMaterial == GetComponent<Renderer>().sharedMaterial){
				cylinderCount++;
					Debug.Log("cylinderCount plus plus " + cylinderCount);

				if(cylinderCount >= LevelManager.instance.obstacleThreshold){
					TetrisMode.UpdateFloor(gameObject);
				}
			}
		}*/
	}
}